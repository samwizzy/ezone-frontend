// import { selectWorkOrderPageDomain } from '../selectors';

describe('selectWorkOrderPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

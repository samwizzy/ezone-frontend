/*
 * AuthorizationPage Messages
 *
 * This contains all the text for the AuthorizationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthorizationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthorizationPage container!',
  },
});

// import { selectAuthorizationPageDomain } from '../selectors';

describe('selectAuthorizationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

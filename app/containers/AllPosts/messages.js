/*
 * AllPosts Messages
 *
 * This contains all the text for the AllPosts container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllPosts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllPosts container!',
  },
});

// import { selectEmployeePageDomain } from '../selectors';

describe('selectEmployeePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

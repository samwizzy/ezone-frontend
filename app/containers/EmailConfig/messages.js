/*
 * EmailConfig Messages
 *
 * This contains all the text for the EmailConfig container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmailConfig';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailConfig container!',
  },
});

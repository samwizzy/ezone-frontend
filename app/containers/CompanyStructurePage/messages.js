/*
 * OrgPage Messages
 *
 * This contains all the text for the OrgPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrgPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrgPage container!',
  },
});

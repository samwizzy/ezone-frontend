/*
 * Config Messages
 *
 * This contains all the text for the Config component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Config';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Config component!',
  },
});
